# ARID 双模型模式 — 发布包使用说明

本文档是随发布包 `ARID-dualmodel-v<版本>.zip` 分发的用户指南（UTF-8 编码，中文）。
解读压后目录结构：

```
ARID-dualmodel-v<版本>/
  workspace/          # ARID 仓库核心文件（双模型预设的源码、角色模型、文档）
  preset/             # 4 个 DSH agent 预设文件（安装时复制到 ~/.dsh）
  config-gui/         # 配置 GUI（server.mjs + feishu-env.mjs + index.html + vendor/yaml）
  feishu-worker/      # 飞书远程控制 worker（独立部署单元，见「五、飞书远程控制」）
  install.bat         # 一键安装（Node/dsh 检测 + 预设 + 骨架 settings + 可选飞书依赖）
  configure.bat       # 启动配置 GUI（自动开浏览器）
  start-arid.bat      # 从 workspace 启动 DSH 工作台
  使用说明.md          # 本文档
```

> 提示：本包在打包机（本机）上制作。可整体拷贝到任意 Windows 电脑使用，
> 不依赖打包机的模型密钥——密钥是本机在配置 GUI 里填的。

---

## 一、安装

1. **确保已安装 Node.js 18.19+**。没有的话先装：
   - 命令行自动安装：`winget install OpenJS.NodeJS.LTS`
   - 或到 <https://nodejs.org/en/download/prebuilt-installer> 手动下载 LTS 安装包。
2. **双击 `install.bat`**。它会：
   - 检测 Node；缺则提示安装后重跑。
   - 若未装 DSH，自动执行 `npm install -g @deepseek-ai/dsh`。
   - 把 `preset\` 下 4 个预设文件复制到
     `%DSH_HOME%\.agent-presets\arid-dualmodel\`（未设 `DSH_HOME` 时为
     `%USERPROFILE%\.dsh\.agent-presets\arid-dualmodel\`）。
   - 若 `%USERPROFILE%\.dsh\settings.yaml` 不存在，创建一个最小骨架（含注释，
     不写任何密钥/模型硬编码）。
   - 若包内有 `feishu-worker\`，会询问是否安装飞书 worker 依赖（需联网）；选 `Y` 则
     `npm install`（仅装 `@larksuiteoapi/node-sdk` + `tsx`），失败仅警告不中断。
   - 幂等：重复运行不会报错，已存在的文件不会覆盖。

   > Node 在哪？安装后它会出现在 PATH。若 `install.bat` 里的 `where node` 找不到，
   > 关掉并重开一个命令行窗口再试（PATH 刷新）。

---

## 二、配置两个模型

双击 **`configure.bat`**，会启动配置服务器（默认 `127.0.0.1:3456`）并自动在
独立窗口打开配置页。界面主要块：规划/审查、执行、OpenAI 兼容网关、
**飞书远程控制**（可选）、保存配置。

### 1. 规划 / 审查（高等级）

| 字段 | 说明 |
|---|---|
| Provider（路由） | 下拉，列出所有已配置路由（含你新建的网关）。决定用哪家的模型端点。 |
| 模型名 | 选定 Provider 后自动联想该路由的 models，也可手输任何模型 id。 |
| 思考程度 | `off / low / medium / high / max`。写入 `agent-default-model.reasoningEffort`；不支持思考的模型请选 `off` 或「不设置」。 |
| API key | 密码框，留空=不修改；非空且长度≥8 才写入。 |

### 2. 执行（低等级）

字段同上。**特别说明**：执行子代理不支持 `reasoningEffort`，其思考程度必须写在
**route 级** `llm-pi-ai.providers.<route>.reasoning`。
如果你让执行与规划**共用同一个 route 但思考程度不同**，保存时工具会**自动派生
独立路由 `<route>-exec`**（复制原路由字段并加上该 `reasoning`），并把执行 provider
指向派生路由，避免两者互相污染。保存成功后会提示派生了哪个路由。

### 3. OpenAI 兼容网关

点「＋ 新建 OpenAI 兼容网关」新增一条手写 route：

| 字段 | 说明 |
|---|---|
| 路由名 | 如 `molifang`，会成为 settings.yaml 里 `llm-pi-ai.providers` 的键，也是下拉里显示的 route。 |
| 显示名 | 展示用，如 `魔方网关`。 |
| 协议 api | 下拉选 `openai-completions`（OpenAI 兼容网关）。 |
| baseURL | 必须 `http(s)://` 开头，如 `https://example.com/v1`。 |
| models | 每行一个模型 `id`（可选 `contextWindow`），如 `gpt-5.6-sol` / 200000。 |
| API key | 该网关的密钥，写入其 `apiKeyEnv` 对应的凭据键。 |

> baseURL 就绪后可让模型下拉直接把 base_url/models 写进 settings.yaml：

```yaml
llm-pi-ai:
  providers:
    molifang:
      displayName: 魔方网关
      apiKeyEnv: GATEWAY_API_KEY   # 由工具自动生成，或你手填
      api: openai-completions
      baseURL: https://example.com/v1
      models:
        - id: gpt-5.6-sol
          contextWindow: 200000
```

### 4. 飞书远程控制（可选）

用飞书机器人远程驱动 DSH（命令：`改 目录 任务` / `状态` / `停止`），只在白名单目录内执行。
**它与上方双模型配置相互独立**——飞书任务走 `dsh --profile headless`，使用自己的模型配置。

该卡片在「保存配置」之前。字段：

| 字段 | 说明 |
|---|---|
| App ID | 必填，`cli_` 开头。来自飞书开放平台自建应用。 |
| App Secret | 密码框，留空=不修改；非空且长度≥8 才写入。 |
| Encrypt Key | 可选，开启「加密推送」时填。 |
| 白名单目录 | 每行一个目录；只有这些目录内的任务才会执行，未配置则远程执行禁用。可含中文路径。 |
| Bot Open ID | 可选，群聊严格 @ 匹配时填机器人 open_id。 |
| 超时秒 / 回复最大字符 / 输出最大行 | 可选；空=不设置。 |
| 高/低等级模型 | 飞书 worker 自己的规划模型与执行模型（provider/model/baseURL + API key）。低等级可选，留空则单模型/用 profile 默认。 |

保存成功后页面会提示「飞书配置已保存，运行 `feishu-worker\start-feishu.bat` 启动长连接」。

### 保存与生效

点「保存配置」。成功后页面会刷新显示最新状态，并提示：
**重启 dsh web（start-arid.bat）或新建会话后生效**。执行模型是每次调用
`subagent_executor` 实时读取的，但也请重启以求稳妥。

保存写入的两个文件：

- `%USERPROFILE%\.dsh\settings.yaml` — 合并写回，**其它顶层段原样保留**，只动
  `llm-pi-ai.providers` / `agent-default-model` / `arid-dual-model.executor` 相关部分。
- `%USERPROFILE%\.dsh\.credentials.yaml` — 只新增/变更对应键，不删除其它键。
  **GUI 与日志绝不回显明文 key**，只显示脱敏状态（已配置/未配置）。

---

## 三、启动工作台

双击 **`start-arid.bat`**：

- 默认工作目录是包内 `workspace\`（存在时），否则用脚本所在目录。
- 启动 `dsh web`（端口 3080，可用 `ARID_PORT` 覆盖）并用 app 模式浏览器打开。
- 幂等：已在运行就直接打开窗口。

在 DSH 工作台新建会话时，选择 agent 预设 **「ARID 双模型模式」**（由
`arid-dualmodel` 提供）。之后：

- **规划阶段**：主 agent 用规划模型研究上下文、出方案。
- **执行阶段**：主 agent 调 `subagent_executor` 工具，把落地编辑 + 跑测试委派给执行模型。
- **失败升级**：反复失败会升级回规划重新出方案。

---

## 四、飞书远程控制

在 `configure.bat` 的配置页里填好「📱 飞书远程控制」卡片并保存（见「二、配置两个模型」的第 4 小节），
然后用以下步骤启动长连接。

### 1. 准备：飞书开放平台

1. 打开 <https://open.feishu.cn> ，进入开发者后台 → 创建 **企业自建应用**。
2. 应用功能 → **机器人** → 开启「机器人」能力。
3. **事件订阅**：选择 **长连接模式**（无需公网回调地址/HTTPS）。配置「接收消息」事件
   `im.message.receive_v1`（需要相应权限）。
4. 申请并开通权限，如 `im:message`、`im:message.group_at_msg`（群聊机器人）、
   `im:chat` 等（用于 @ 触达）。发布应用版本并等待审核通过。
5. 复制 **App ID**（`cli_` 开头）与 **App Secret**，填到配置 GUI 的「飞书远程控制」卡片。
   若开启了「加密推送」，再复制 Encrypt Key。
6. 获取机器人 **open_id**（在开发者后台「机器人」设置里可找到 `ou_` 开头的 id），
   如需群聊严格 @ 匹配就填到 Bot Open ID。

### 2. 在配置 GUI 填写并保存

- App ID / App Secret 必填。
- **白名单目录**：每行一个，填你想通过飞书远程操作的仓库目录（可含中文）。**未配置白名单则远程执行禁用。**
- 可选：Bot Open ID、超时/回复参数、飞书 worker 自己的高/低等级模型。
- 点「保存配置」。成功后提示运行 `feishu-worker\start-feishu.bat`。

> GUI 会把飞书配置写到 `~/.dsh/settings.yaml`（`arid-feishu` 段）与 `~/.dsh/.credentials.yaml`
> （各 API key），并由 `config-gui\feishu-env.mjs` 生成 `feishu-worker\.env`。

### 3. 启动长连接

1. 先运行 `install.bat`；当询问「Install Feishu worker dependencies」时选 `Y`，
   它会在 `feishu-worker\` 里 `npm install`（安装 `@larksuiteoapi/node-sdk` 与 `tsx`）。
   若你跳过了，也可在命令行进入 `feishu-worker\` 后手动 `npm install`。
2. 双击 **`feishu-worker\start-feishu.bat`**。它会：
   - 用配置 GUI 的飞书设置**重新生成** `feishu-worker\.env`；
   - 然后启动长连接 worker（官方 lark 长连接，**无需公网 IP**）。
3. **保持该窗口打开**：窗口开着 = 长连接运行中；关闭窗口 = 停止 worker。

连接建立后，在飞书里私聊你的机器人即可使用命令：

```
改 E:\myrepo 修复登录 bug
状态
停止
```

> 群聊需 @ 机器人；单聊直接发命令。

### 4. 常见问题

- **长连接连不上？** 检查 App ID/Secret 是否填对、应用是否已发布、事件订阅是否选了长连接模式
  （`im.message.receive_v1`）。worker 窗口会打印连接错误。
- **任务不执行？** 多半是白名单未配置，或目标目录不在 `ARID_FEISHU_ALLOWED_DIRS` 里。
- **提示飞书依赖未安装？** 运行 `npm install`（feishu-worker 目录）或重跑 `install.bat` 选 Y。
- **与双模型配置的关系？** 飞书 worker 与 DSH 工作台（start-arid.bat）**相互独立**。飞书任务用
  `dsh --profile headless` 跑，用的是「飞书远程控制」卡片里**自己那组**高/低等级模型，
  与上方规划/执行卡片无关。

---

## 五、常见问题

**Q1. 双击 `start-arid.bat` 窗口一闪就没了？**
批处理必须纯 ASCII + CRLF。本发布包的 bats 已是 ASCII+CRLF，若你还是遇到，多半是
`dsh` 未装（报错会 pause 显示 `dsh was not found`）。先跑 `install.bat`。

**Q2. 配置页打不开 / 端口被占？**
默认 3456。可用 `set ARID_GUI_PORT=4567` 先设端口，再运行 `configure.bat`。
若已有旧服务在跑，configure.bat 会复用并直接开浏览器。

**Q3. 填了 key 但模型报「Provider is not configured」？**
Key 存在 `~/.dsh/.credentials.yaml` 的 `apiKeyEnv` 对应键下。检查 settings.yaml 里该
route 的 `apiKeyEnv` 是否与你填的 key 对应；DSH 通过该环境变量名去解析凭据。

**Q4. 思考程度不生效？**
- 规划模型的思考程度在 `agent-default-model.reasoningEffort`（DeepSeek 类适配器一般只认
  `high`/`max`，请按模型能力选）。
- 执行模型的思考程度是 route 级 `reasoning`。与规划**同 route 且不同思考度**时，
  工具会自动派生 `<route>-exec`——若没派生，说明两者思考度相同或执行未设思考度。
- 改完请重启 dsh web / 新建会话。

**Q5. 想用飞书远程控制？**
飞书远程控制是一个独立 worker。在配置 GUI 的「飞书远程控制」卡片填好设置并保存，
运行 `feishu-worker\start-feishu.bat` 启动长连接即可。详见「四、飞书远程控制」。
（也可直接使用 `workspace` 里的 `.env.example` 与 `src/feishu/` 手动配置。）

---

## 六、卸载 / 重置

- 删除预设目录：`%DSH_HOME%\.agent-presets\arid-dualmodel\`（未设 `DSH_HOME` 时为
  `%USERPROFILE%\.dsh\.agent-presets\arid-dualmodel\`）。
- 从 `settings.yaml` 删除 `agent-default-model`、`arid-dual-model`（执行模型段），
  以及自行添加的网关 route。
- 从 `%DSH_HOME%\.credentials.yaml` 删除对应凭据键（未设 `DSH_HOME` 时为 `~/.dsh/.credentials.yaml`）。

---

## 七、技术说明（写给高级用户）

- 配置 GUI 是 **Node ESM 零依赖服务器**（内置模块 + 复制自 `node_modules/yaml` 的
  vendored yaml），仅监听 `127.0.0.1`。可用 `ARID_CONFIG_HOME` 覆盖配置目录
  （主要供自动化测试 / 便携场景）。
- DSH CLI **没有** settings 子命令，本 GUI 直接读写 YAML 文件，所以**必须重启 dsh web
  才生效**。
- 凭据写入用**原子写**（临时文件 + rename），避免写一半损坏配置。
- 飞书 `.env` 由 `config-gui\feishu-env.mjs` 从 settings + credentials 生成（`--print`
  可预览、key 掩码），值为 UTF-8（允许中文路径）。worker 主入口会在启动时自动加载
  `feishu-worker\.env`（路径可用 `ARID_FEISHU_ENV` 覆盖，默认当前目录 `.env`；
  只覆盖不存在的环境变量，等价 dotenv 语义）。
