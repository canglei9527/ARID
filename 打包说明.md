# 打包说明

生成「ARID 双模型模式」发布包（可拷贝到其它 Windows 电脑直接使用）。

## 前置条件

- 本仓库已 `npm install`（因为要把 `node_modules/yaml` 的 `dist/` + `package.json`
  **复制**进 `config-gui/vendor/yaml/` 供打包，保证发布包零外部依赖）。
- 本机 `~/.dsh/.agent-presets/arid-dualmodel/` 下存在 4 个预设文件：
  `preset.yml`、`agent.cordis.yml`、`arid-dualmodel-ext6.cjs`、`arid-tavily.cjs`。
- PowerShell（Windows 自带）。

## 打包

在仓库根运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\pack-release.ps1
```

脚本行为：

1. 从 `package.json` 读版本号（如 `0.1.0`）。
2. 组装 `dist/ARID-dualmodel-v<版本>/`：
   - `workspace/` —— 仓库核心文件（排除 `node_modules`、`dist`、`kb-installer`、
     `.aider*`、`.git`、`.env`、`feishu-worker`、本地杂项；保留 `src/`、`test/`、`docs/`、
     `AGENTS.md`、`package.json`、`tsconfig.json`、`.env.example`）。
   - `preset/` —— 从 `~/.dsh/.agent-presets/arid-dualmodel/` 复制 4 个预设文件。
   - `config-gui/` —— `server.mjs`、`feishu-env.mjs`、`index.html`、`vendor/`（vendored yaml）。
   - `feishu-worker/` —— 独立部署的飞书远程控制 worker：`package.json`、`.env.example`、
     `start-feishu.bat`，以及 `src/feishu/`（bot/text/worker-entry/worker-main/lark-transport/
     dsh-executor/env.ts）加 `src/roles.ts`。
   - `install.bat`、`configure.bat`、`start-arid.bat`、`使用说明.md`。
3. 用 `Compress-Archive` 打成 `dist/ARID-dualmodel-v<版本>.zip`。

结束后会打印包目录、zip 路径与大小。

## 验证（可选）

```powershell
# 列出 zip 里的文件，确认结构齐全
powershell -Command "Add-Type -AssemblyName System.IO.Compression.FileSystem; \
  [IO.Compression.ZipFile]::OpenRead('dist\ARID-dualmodel-v<版本>.zip').Entries | \
  Select-Object -ExpandProperty FullName"
```

## 说明

- 打包脚本 `scripts/pack-release.ps1` 内容保持 **纯 ASCII**（PowerShell 5.1 兼容）。
  包目录名/zip 名用英文 + 版本号，中文说明都写在 `使用说明.md` 与 GUI 页面里。
- 包内 `start-arid.bat` 已内置「存在 `%~dp0workspace` 就用它」的逻辑，因此打包时
  直接复制仓库根同名文件即可，无需在脚本里做字符串替换。
